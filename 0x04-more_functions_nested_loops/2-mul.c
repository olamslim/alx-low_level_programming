#include "main.h"

/**
 * mul - checks if character has a function that multiples integers
 * @a: first integer
 * @b: second integer
 * Return: the multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
